<Window x:Class="SunloginManager.AddConnectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SunloginManager"
        mc:Ignorable="d"
        Title="添加远程连接" 
        Height="550" Width="550"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F7"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- 苹果风格按钮样式 -->
        <Style x:Key="AppleButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007AFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0051D5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004BB5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 苹果风格文本框样式 -->
        <Style x:Key="AppleTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#D1D1D6"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="45"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#007AFF"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="#007AFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="White" CornerRadius="12" Margin="20" Padding="20">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.1" ShadowDepth="0" BlurRadius="15"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 标题 -->
            <TextBlock Grid.Row="0" Text="添加新的远程连接" FontSize="20" FontWeight="SemiBold" 
                       Margin="0,0,0,20" Foreground="#1D1D1F" HorizontalAlignment="Center"/>
            
            <!-- 表单内容 -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock Text="连接名称:" Margin="0,0,0,5" FontWeight="Medium" Foreground="#1D1D1F"/>
                <TextBox x:Name="NameTextBox" Style="{StaticResource AppleTextBoxStyle}" Height="45" Margin="0,0,0,15"/>
                
                <TextBlock Text="识别码:" Margin="0,0,0,5" FontWeight="Medium" Foreground="#1D1D1F"/>
                <TextBox x:Name="IdentificationCodeTextBox" Style="{StaticResource AppleTextBoxStyle}" Height="45" Margin="0,0,0,15"/>
                
                <TextBlock Text="连接码:" Margin="0,0,0,5" FontWeight="Medium" Foreground="#1D1D1F"/>
                <TextBox x:Name="ConnectionCodeTextBox" Style="{StaticResource AppleTextBoxStyle}" Height="45" Margin="0,0,0,15"/>
                
                <TextBlock Text="备注 (可选):" Margin="0,0,0,5" FontWeight="Medium" Foreground="#1D1D1F"/>
                <TextBox x:Name="NotesTextBox" Style="{StaticResource AppleTextBoxStyle}" Height="45" Margin="0,0,0,15"/>
            </StackPanel>
            
            <!-- 按钮 -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Style="{StaticResource AppleButtonStyle}" Width="80" Height="35" 
                        Background="#8E8E93" Click="CancelButton_Click"/>
                <Button Content="确定" Style="{StaticResource AppleButtonStyle}" Width="80" Height="35" 
                        Click="OKButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>