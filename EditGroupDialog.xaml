<Window x:Class="SunloginManager.EditGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑分组" Height="491" Width="481"
        WindowStartupLocation="CenterOwner"
        Background="#F2F2F7"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Border Background="White" CornerRadius="12" Padding="28,24">
            <Border.Effect>
                <DropShadowEffect Color="#000000" Opacity="0.08" ShadowDepth="0" BlurRadius="20"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- 标题 -->
                <TextBlock Grid.Row="0" Text="编辑分组" FontSize="19" FontWeight="SemiBold" 
                           Margin="0,0,0,20" Foreground="#1D1D1F"/>

                <!-- 表单内容 -->
                <StackPanel Grid.Row="1" Margin="0,0,0,18">
                    <!-- 分组名称 -->
                    <TextBlock Text="分组名称" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="#1D1D1F"/>
                    <Border Background="White"
                            BorderBrush="#E5E5EA"
                            BorderThickness="1"
                            CornerRadius="6"
                            Height="38"
                            Margin="0,0,0,12">
                        <TextBox x:Name="NameTextBox" 
                                 Padding="10,0"
                                 FontSize="13"
                                 VerticalAlignment="Center"
                                 VerticalContentAlignment="Center"
                                 Background="Transparent"
                                 BorderThickness="0"/>
                    </Border>

                    <!-- 分组描述 -->
                    <TextBlock Text="分组描述" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="#1D1D1F"/>
                    <Border Background="White"
                            BorderBrush="#E5E5EA"
                            BorderThickness="1"
                            CornerRadius="6"
                            Height="70"
                            Margin="0,0,0,12">
                        <TextBox x:Name="DescriptionTextBox" 
                                 Padding="10,8"
                                 FontSize="13"
                                 TextWrapping="Wrap"
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Background="Transparent"
                                 BorderThickness="0"/>
                    </Border>

                    <!-- 分组颜色 -->
                    <TextBlock Text="分组颜色" FontSize="13" FontWeight="SemiBold" Margin="0,0,0,6" Foreground="#1D1D1F"/>
                    <ComboBox x:Name="ColorComboBox"
                              Padding="10,0"
                              FontSize="13"
                              Height="38"
                              VerticalContentAlignment="Center"
                              Background="White"
                              BorderBrush="#E5E5EA"
                              BorderThickness="1"
                              SelectedIndex="0">
                        <ComboBox.Style>
                            <Style TargetType="ComboBox">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBox">
                                            <Grid>
                                                <ToggleButton x:Name="ToggleButton"
                                                              Background="White"
                                                              BorderBrush="#E5E5EA"
                                                              BorderThickness="1"
                                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                              ClickMode="Press">
                                                    <ToggleButton.Template>
                                                        <ControlTemplate TargetType="ToggleButton">
                                                            <Border x:Name="border"
                                                                    Background="White"
                                                                    BorderBrush="#E5E5EA"
                                                                    BorderThickness="1"
                                                                    CornerRadius="6"
                                                                    Padding="10,0">
                                                                <Grid>
                                                                    <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                                    <Path x:Name="Arrow"
                                                                          HorizontalAlignment="Right"
                                                                          VerticalAlignment="Center"
                                                                          Data="M 0 0 L 4 4 L 8 0 Z"
                                                                          Fill="#8E8E93"/>
                                                                </Grid>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="border" Property="BorderBrush" Value="#007AFF"/>
                                                                </Trigger>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="border" Property="BorderBrush" Value="#007AFF"/>
                                                                    <Setter TargetName="Arrow" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </ToggleButton.Template>
                                                </ToggleButton>
                                                <ContentPresenter x:Name="ContentSite"
                                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                                  Margin="10,0,30,0"
                                                                  VerticalAlignment="Center"
                                                                  HorizontalAlignment="Left"
                                                                  IsHitTestVisible="False"/>
                                                <Popup x:Name="Popup"
                                                       Placement="Bottom"
                                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                                       AllowsTransparency="True"
                                                       Focusable="False"
                                                       PopupAnimation="Slide">
                                                    <Border Background="White"
                                                            BorderBrush="#E5E5EA"
                                                            BorderThickness="1"
                                                            CornerRadius="6"
                                                            Margin="0,4,0,0">
                                                        <Border.Effect>
                                                            <DropShadowEffect Color="#000000" Opacity="0.1" ShadowDepth="2" BlurRadius="10"/>
                                                        </Border.Effect>
                                                        <ScrollViewer Margin="0,4">
                                                            <ItemsPresenter/>
                                                        </ScrollViewer>
                                                    </Border>
                                                </Popup>
                                            </Grid>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.Style>
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0">
                                    <Ellipse Width="18" Height="18" Margin="0,0,10,0">
                                        <Ellipse.Fill>
                                            <SolidColorBrush Color="{Binding}"/>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                    <TextBlock Text="{Binding}" VerticalAlignment="Center" FontSize="13"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                        <ComboBox.ItemContainerStyle>
                            <Style TargetType="ComboBoxItem">
                                <Setter Property="Padding" Value="10,8"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ComboBoxItem">
                                            <Border x:Name="border"
                                                    Background="{TemplateBinding Background}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Stretch"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#F0F8FF"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#E6F2FF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ComboBox.ItemContainerStyle>
                    </ComboBox>
                </StackPanel>

                <!-- 按钮 -->
                <StackPanel Grid.Row="2" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button Content="取消" 
                            Click="CancelButton_Click"
                            Margin="0,0,10,0"
                            Padding="18,8"
                            Width="75"
                            Height="34"
                            Background="#E5E5EA"
                            Foreground="#1D1D1F"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#D1D1D6"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <Button Content="保存" 
                            Click="SaveButton_Click"
                            Padding="18,8"
                            Width="75"
                            Height="34"
                            Background="#007AFF"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#0051D5"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
